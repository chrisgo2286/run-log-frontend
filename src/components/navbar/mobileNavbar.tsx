export default function MobileNavbar (): JSX.Element {
    return (
        <nav className="sm:hidden fixed top-0 w-screen bg-blue-100 flex justify-between">
            <i className="material-icons">menu</i>
        </nav>
    )
}