export type CardProps = {
    headlineText: string,
    detailText: string,
    link: JSX.Element
}

export type CardSectionProp = {
    text: string
}

export type CardFooterProp = {
    link: JSX.Element
}